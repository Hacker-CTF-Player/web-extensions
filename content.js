// Find all input fields and set their value to "Hello ROHIT"
document.querySelectorAll('input[type="text"], input[type="email"], input[type="password"], textarea').forEach(function(input) {
    input.value = "Hello ROHIT";
});
